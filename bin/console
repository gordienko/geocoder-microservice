#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative '../config/environmant'

puts "Loading #{Application.environment} environment (Sinatra) #{Sinatra::VERSION}"

require 'irb'

IRB.start(__FILE__)
